machine:
  node:
    version: 7

dependencies:
  pre:
    - curl -o- -L https://yarnpkg.com/install.sh | bash
    - yarn global add gulp cypress-cli
  override:
    - yarn install

test:
  override:
    - yarn tslint
    - yarn test
